import React, { useState } from "react";

const RestartGameButton = () => {
  return (
    <div onClick={(e) => console.log("restart modal needed")}>Restart Game</div>
  );
};

export default RestartGameButton;
